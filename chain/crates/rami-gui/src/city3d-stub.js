// Sustituto temporal: el cliente 3D real (city3d.js) se incluye en la siguiente
// entrega; mientras tanto el panel usa la vista 2D.
window.RamiCity3D = { mount: function(){ throw new Error("WebGL: cliente 3D no incluido en esta build"); } };
