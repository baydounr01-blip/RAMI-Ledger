# Rust
chain/target/
**/*.rs.bk
# El Cargo.lock del workspace SÍ se versiona: los binarios (nodo/monedero) que
# se distribuyen deben construirse de forma reproducible con --locked.
# RAMI ledger secretos (NUNCA se publican)
**/secrets.json
**/.tmp_*
rami_ledger/
# Node/Netlify
web/node_modules/
web/.netlify/
# binarios de wallet compilados para distribución (se suben como release, no al repo)
dist/
